global:
  resolve_timeout: 1m
  smtp_require_tls: false
receivers:
  - name: 'default'
    webhook_configs: []
route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: 'default'
 

 
